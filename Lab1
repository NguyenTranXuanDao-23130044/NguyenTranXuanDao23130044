
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản Lý Môn Học</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: #f0f0f0;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #4CAF50;
            color: white;
        }
        input[type="text"], input[type="button"] {
            padding: 8px;
            margin: 5px 0;
            width: 100%;
        }
        .btn {
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
        }
    </style>
</head>
<body>

<h1>Quản Lý Môn Học</h1>

<input type="text" id="search" placeholder="Tìm kiếm môn học..." onkeyup="searchSubject()">
<br>
<input type="text" id="subjectName" placeholder="Tên môn học">
<input type="button" class="btn" value="Thêm Môn Học" onclick="addSubject()">

<table id="subjectTable">
    <thead>
        <tr>
            <th>Tên Môn Học</th>
            <th>Xóa Môn Học</th>
        </tr>
    </thead>
    <tbody>
        <!-- Danh sách môn học sẽ được thêm vào đây -->
    </tbody>
</table>

<script>
    let subjects = [];

    function addSubject() {
        const subjectInput = document.getElementById('subjectName');
        const subjectName = subjectInput.value.trim();

        if (subjectName === '') {
            alert('Vui lòng nhập tên môn học.');
            return;
        }

        subjects.push(subjectName);
        subjectInput.value = '';
        renderSubjects();
    }

    function renderSubjects() {
        const tbody = document.getElementById('subjectTable').getElementsByTagName('tbody')[0];
        tbody.innerHTML = '';

        subjects.forEach((subject, index) => {
            const row = tbody.insertRow();
            const cell1 = row.insertCell(0);
            const cell2 = row.insertCell(1);

            cell1.textContent = subject;
            cell2.innerHTML = `<button onclick="editSubject(${index})">Sửa</button> <button onclick="deleteSubject(${index})">Xóa</button>`;
        });
    }

    function editSubject(index) {
        const newSubject = prompt('Nhập tên môn học mới:', subjects[index]);
        if (newSubject) {
            subjects[index] = newSubject;
            renderSubjects();
        }
    }

    function deleteSubject(index) {
        subjects.splice(index, 1);
        renderSubjects();
    }

    function searchSubject() {
        const searchInput = document.getElementById('search').value.toLowerCase();# NguyenTranXuanDao23130044
